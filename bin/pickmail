#!/usr/bin/env python

import pickmail

config = pickmail.Config()

parser = pickmail.Parser()
config = parser.parse_cli(config)

connection = pickmail.Connection(config)

count = int(config.batchsize)
offset = 0
selected = -1
while selected < 0:
    print connection.get_messages(count=count, offset=offset) 
    msg = raw_input('Please select a message, or press return for more:\n')
    if msg == '':
        offset = offset + config.batchsize 
        print connection.get_messages(count=count, offset=offset)
    else:
        selected = msg

if selected != '':
    data = connection.data
    uid = data[int(selected)]['uid']
    file = open('pickmail.out', 'w')
    file.write(connection.fetch_message(uid))
    file.close()
